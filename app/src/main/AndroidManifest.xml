<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.androidaccelarate.guiadebolsosustentavel">

    <application
        android:allowBackup="true"
        android:icon="@drawable/icon_launcher"
        android:label="@string/app_name"
        android:roundIcon="@drawable/icon_launcher"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity
            android:name=".MainActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".CuriosidadesActivity"
            android:label="@string/CR_Tittle" />

        <activity
            android:name=".ColetaSeletivaActivity"
            android:label="@string/CS_Tittle" />

        <activity
            android:name=".DicasSustentaveisActivity"
            android:label="@string/DS_Tittle" />

        <activity
            android:name=".DicasSustentaveis2Activity"
            android:label="@string/DS_Tittle" />

        <activity
            android:name=".DicasSustentaveis3Activity"
            android:label="@string/DS_Tittle" />

    </application>

</manifest>